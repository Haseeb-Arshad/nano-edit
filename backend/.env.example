PORT=8000
DEV_TOKEN=dev-token
PROVIDER=mock

# Only needed for real provider
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# S3 / MinIO
S3_ENDPOINT=http://127.0.0.1:9000
S3_REGION=us-east-1
S3_BUCKET=ai-edits
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_USE_PATH_STYLE=true
S3_PRESIGN_TTL_SECONDS=300

# Redis
REDIS_URL=redis://127.0.0.1:6379

# Quotas / Budget
DAILY_BUDGET_MAX_CALLS=100
USER_DAILY_QUOTA=50

# Cost model (cents per MB)
COST_PER_INPUT_MB_CENTS=20
COST_PER_OUTPUT_MB_CENTS=10

