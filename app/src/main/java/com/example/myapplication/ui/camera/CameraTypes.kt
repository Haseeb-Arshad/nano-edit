package com.example.myapplication.ui.camera

/**
 * Camera mode enum - shared across camera components
 */
enum class CameraMode {
    PHOTO, VIDEO
}

/**
 * Flash mode enum
 */
enum class FlashMode {
    OFF, ON, AUTO
}
